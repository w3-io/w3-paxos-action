{
  "name": "w3-paxos-action",
  "version": "0.1.0",
  "private": true,
  "description": "Paxos institutional crypto \u2014 identity, custody, trading, stablecoins, settlement, orchestration for W3 workflows",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@w3-io/action-core": "^0.3.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
